[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"

[packages]

[requires]
python_version = "{{'.'.join(cookiecutter.python_version.split('.')[:2])}}"
